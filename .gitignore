# Terraform state — contains real IPs and credentials
*.tfstate
*.tfstate.*
*.tfstate.backup
.terraform/
.terraform.lock.hcl
crash.log

# Variables file — contains VirusTotal API key and your IP
terraform.tfvars

# SSH keys
*.pem
*.key

# OS files
.DS_Store
Thumbs.db
terraform.tfvars
terraform/terraform.tfvars
**/terraform.tfvars
# AWS CLI installer files
aws/
awscliv2.zip
